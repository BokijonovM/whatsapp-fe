export interface IUser {
  username: string;
  email: string;
  avatar: string;
  refreshToken: string;
}

export interface ISelectedUser {
  username: string;
  email: string;
  avatar: string;
  // createdAt: Date;
  // updatedAp: Date;
  // _id: string;
}
